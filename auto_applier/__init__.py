"""Auto Applier v3 — ground-up rewrite.

v3 lives in this ``av3`` package, deliberately separate from the v2 ``auto_applier``
package so v2 stays runnable as a porting reference (see ``docs/v3-architecture.md`` §3
and the package-root decision recorded there). v2 is lessons, not a base to extend —
port *logic*, not structure.
"""

__version__ = "3.0.0a0"
